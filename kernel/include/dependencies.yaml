# Used to generate dependencies.h

domain: kernel

conditional:
    dependencies:
        error_handling:
            needs: [sched_stop_self]
        thread_suspend:
            needs: [sched_stop_self]
